import React from "react";
import { useSelector } from "react-redux";


const Title = () => {
  return (
    <div style={{textAlign:'center'}}>
      <h1 className="primary" style={{color:'#f2705a'}}>Students Management</h1>
    </div>
  );
};

export default Title;
